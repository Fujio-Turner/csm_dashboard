# CSM Dashboard roadmap

The living train map is **[`docs/ROADMAP.md`](docs/ROADMAP.md)**.

**Now:** 0.1.51 local desk. Settings stores AI keys + connector tokens in CBL `credentials`.  
**Next:** 0.2 read-only aggregator (live Jira → mail → Slack → Zendesk).  
**Then:** 0.3 tasks as special self-emails (send to yourself after confirm).  
**Later:** slash-in-chat, transcripts → tasks, weekly/monthly/QBR, project briefs + optional Confluence, App Services sync, CBL EE vectors.
